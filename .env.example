# Node-RED connection
NR_URL=http://localhost:1880

# Authentication (choose one method):

# Option 1: Bearer token
# NR_TOKEN=your-node-red-access-token

# Option 2: Basic Auth (username + password)
# NR_USER=admin
# NR_PASS=your-password

# Option 3: No auth (for unsecured local instances)
# Leave all auth variables commented out
